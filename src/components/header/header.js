import './header.css';
function Header() {
  const appStyle={textAlign:"center"};
  return (
    <h1 style={appStyle} className="header">
      First batch is the best batch
    </h1>
  );
      
}

export default Header;
